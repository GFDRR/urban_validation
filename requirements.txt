# Python dependencies for the urban_validation pipeline.
#   pip install -r requirements.txt
# A conda alternative is provided in environment.yaml.
#
# Major versions are pinned for the geospatial stack (API-breaking across
# majors); other packages use tested lower bounds. Versions reflect a working
# environment (geopandas 1.0, shapely 2.1, pandas 2.2, rasterio 1.4).

# --- Core geospatial ---
geopandas>=1.0,<2.0
shapely>=2.0,<3.0
fiona>=1.9,<2.0
pyproj>=3.6,<4.0
rasterio>=1.3,<2.0
pyogrio>=0.7
rtree>=1.0

# --- Data / numerics ---
numpy>=1.24,<2.0
pandas>=2.0,<3.0
pyyaml>=6.0

# --- Plotting / visualization ---
matplotlib>=3.7,<4.0
seaborn>=0.12
plotly>=5.0,<6.0
kaleido>=0.2            # static image export for plotly (nb 91)
contextily>=1.3        # basemaps (nb 97/98)
matplotlib-scalebar>=0.8
folium>=0.14

# --- Statistics (analysis notebooks) ---
scipy>=1.10
statsmodels>=0.14
scikit-posthocs>=0.9

# --- Download / IO ---
earthengine-api>=0.1.380   # provides `import ee`
duckdb>=1.0
requests>=2.28
openpyxl>=3.1,<4.0         # pandas Excel engine (.xlsx read/write)
psutil>=5.9

# --- Geo helpers (map / reporting notebooks) ---
pycountry>=22.0
geodatasets>=2023.3

# --- Notebooks ---
jupyter>=1.0,<2.0
ipython>=8.0

# --- Colab-only (provided by the Colab runtime; not needed for a local install) ---
# google-colab
# google-api-python-client   # nb 99
# google-auth                # nb 99
